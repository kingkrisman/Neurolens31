node_modules/
.project_id
.github_repo
.env
.env.*

# Build output. Regenerated by `npm run build`; tracking it churned ~44 files
# into every commit and made most of the old repo's history build artifacts.
.vercel/output/
.output/
.nitro/
.tanstack/
dist/
.node_modules.lock

.vercel

# Development detritus, not source. Kept on disk, out of the repository:
# 23 MB of it made pushes time out over a slow link.
screenshots/
attachments/

# Local dev server log (started detached on Windows)
dev-server.log

# Generated on every build by scripts/build-seo-files.mjs
public/robots.txt
public/sitemap.xml

# Supabase personal access token, used by scripts/supabase-apply.mjs
.supabase-token
